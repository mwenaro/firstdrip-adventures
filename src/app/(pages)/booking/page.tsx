import { BookingForm } from "@/components/custom/BookingForm";

export default function BookingPage() {
  return (
    <div className="pt-20">
      <BookingForm destination={undefined} />
    </div>
  );
}
